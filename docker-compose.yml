services:
  postgres:
    image: postgres:16
    container_name: louvor-manager-postgress
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: louvor-manager
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: